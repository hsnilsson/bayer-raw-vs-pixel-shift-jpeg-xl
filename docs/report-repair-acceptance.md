# Report repair acceptance record

This repair preserves the research question, strict independent compressed RAW61 byte budgets, and the separate equally prominent 200 MiB DNG route. Its endpoint is a validated local report for owner review. Publication remains a separate action.

| Review finding | Repair and evidence |
| --- | --- |
| Tables and viewers used different encodes and target acquisitions | One release binds 16 current RGB frames, 160 candidates, 22 crop manifests, every measurement, transport payload and preview. The f/4.5 RGB target and historical f/8 DNG/combiner target are identified separately. |
| RGB16 readers lost precision | PPM, PNG and TIFF retain adjacent uint16 codes. Real-image lossless pilots check exact pixels, metadata and ICC interpretation. |
| Wide-gamut rendered RGB was treated as sRGB or linear camera data | Actual embedded ICC curves and matrices determine linearization, XYZ/Lab conversion and sRGB display. LittleCMS and independent browser/Python calculations check the transform. Camera-native DNG errors remain camera-code errors. |
| Exposure and inversions used inconsistent domains | Five declared reference-calibrated transforms act in linear light. Browser exposure acts before the selected transform; exploratory point curves remain outside published metric screens. |
| RAW compression/capture cohorts and 105% allowance distorted size conclusions | Twelve independent compressed film frames form the primary cohort. All other captures are explicit secondary/diagnostic cohorts. The threshold is exactly candidate bytes ≤ paired RAW bytes; collection totals and repeated distance rows have separate denominators. |
| Reduced images were used to imply native-detail preservation | Approved native crops and 10× linear box full-frame diagnostics remain separate. Every scope is retained; native and reduced results cannot overwrite each other in a join. |
| Float32 filtering created artificial constant-field structure | Float64 box accumulation agrees with direct neighborhood calculations and preserves constant fields. Near-zero high-pass reference energy cannot produce an automatic favorable screen. |
| Serial batches could reuse another frame's reference | Frame identity and preparation scope control reuse. Distinct-frame regression fixtures check the serial path. |
| Timestamps and file sizes could validate stale results | Content hashes and stage recipes bind inputs and outputs. Changed content with unchanged size/time is rejected. Old encodes are adopted only through the explicit audit, with historical lineage limitations disclosed. |
| Interruptions lost completed work | Atomic candidate checkpoints, frame receipts and export recovery journals support restart. The complete 16-frame reuse check blocks every heavy function. A partial two-distance interruption test proves that the first completed candidate is reused when the next fails. |
| Fresh ICC timestamps invalidated completed preview checkpoints | The rebuild adapter preserves an existing preview only if pixels and profile/metadata match apart from the ICC creation date. Changed pixels, profile data and corrupt images are rejected. The interrupted-run regression was confirmed to fail without this adapter. |
| Metadata preservation could be assumed instead of checked | Metadata-qualified copies receive measured final byte sizes. All 160 image codestreams were verified unchanged by metadata repair. Required preservation status comes from the audit. Original encodes remain retained. |
| Resampling and crop boundaries could drive the comparison | Source support, registration and common valid pixels are explicit. Reproduced native identity measurements and a two-pixel boundary sensitivity audit changed 0/220 relative decisions. Aligned RAW viewer quantization reaches four display codes across the checked five modes at zero EV; floating-point scientific measurements are unaffected. |
| DNG and combiner results overstated archival quality or latitude | The DNG technical/size results remain, while unsupported camera-RGB DeltaE, cross-domain RAW ratios and archive-value verdicts are withdrawn. The corrected combiner experiment reports reference agreement. Capture-bracket sensor evidence is a separate experiment. |
| Key numerical evidence was private or ignored | Sanitized JSON/CSV, source/artifact hashes, recipes, versions and code identities accompany the report. Full private scans and local paths remain outside the release. Six public fixtures support independent reproduction. |
| Casual viewer interactions prefetched the corpus | The viewer fetches the current crop and at most two adjacent quality levels, cancels obsolete requests and caps pixel/image caches. A direct PS16-reference comparison and physical-pixel scale control are available. |
| Old reports could be regenerated as current | The default renderer requires a complete verified release. Legacy output requires an explicit historical flag and cannot overwrite the current site. Gates reject stale HTML, incorrect labels, mixed evidence and changed recipes. |

The [machine-assisted review record](../site/data/review-notes.md) contains actual validation and visual-review results. The [reproduction instructions](../REPRODUCIBILITY.md) give public and owner-input commands. The [publication audit](../PUBLICATION_AUDIT.md) defines the release and retention boundaries.

Remaining scientific limits include incomplete historical encoder-input hashes, unresolved effective RAW/DNG render settings, PS16 reference rather than scene truth, limited captures, and unvalidated DNG quality superiority. Human review, a calibrated end-to-end DNG quality study, broader captures and eventual publication are not silently treated as completed by this repair.
