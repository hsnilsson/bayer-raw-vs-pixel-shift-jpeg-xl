# Machine-assisted review record

Review date: 2026-09-17. The owner reviewed the repaired report and approved publication on the same date. The initial publication used commit [d42e26a](https://github.com/hsnilsson/bayer-raw-vs-pixel-shift-jpeg-xl/commit/d42e26a97ddd109675d2a29c4db806f2fe1840c2); the subsequent editorial revision used [0220f2a](https://github.com/hsnilsson/bayer-raw-vs-pixel-shift-jpeg-xl/commit/0220f2a54b4c362214818e0a47126dda67ff1e4c). This record describes the machine-assisted numerical and visual checks performed during the repair. Owner approval concerns publication; the visual checks documented below use the stated machine-assisted method.

## Adox f/4.5 crop revision (local, 2026-09-17)

The owner selected four new centers with magenta marks on the existing 956×637 context map and approved the coordinate-rendered preview. The context recipe reduces the original 19136×12752 image in 20×20 blocks, so marker coordinates are multiplied by 20; no image-generation output is used as measurement evidence. Names and the 768×768 source-pixel crop size are retained.

| Crop | Original-image rectangle (x, y, width, height) | Region |
|---|---|---|
| manual-01 | 11336, 4629, 768, 768 | Small central radial target |
| manual-02 | 16713, 2046, 768, 768 | Upper-right diagonal target |
| manual-03 | 12126, 5746, 768, 768 | Dark end of the right grayscale wedge |
| manual-04 | 13718, 2388, 768, 768 | Center of the upper-right circular target |

The same-sequence RAW anchor and retained PS16/JXL sources are reused. Native crop measurements, local registration, shared tone recipes, viewer buffers, context maps, full-frame overview markings and boundary/quantization audits are regenerated. The capture remains in `secondary_same_sequence_target`; this selection does not create an independent RAW61 storage-budget pair. The historical f/8 combiner selections remain separate.

- All ten Adox levels were decoded and measured again, producing 250 records. The 200 native-crop records changed; the 50 Adox full-frame records and the 1,650 records for other captures are unchanged. All 160 retained JXL file hashes and exact byte counts were checked against the preceding release. Full private source and candidate hashes were verified during assembly.
- The four PS16 RGB16 buffers match the selected original TIFF pixels exactly. All 48 Adox RGB16 transports were checked, and the new context map is pixel-identical to the approved coordinate-rendered preview. All 240 Adox full-frame overviews were regenerated with the new crop coordinates and shared tone recipes. Other viewer manifests receive the updated shared overview evidence identity.
- Local registration passed for all four crops, retaining 97.15–98.44% valid support. The re-evaluated crop-boundary audit still changes 0 of 220 defined comparisons; the maximum RAW viewer quantization difference remains four display codes. Primary budget summaries and the separate historical DNG, combiner, public and controlled evidence remain unchanged after review.
- All 212 automated tests passed. The release gate validated 16 frames, 160 candidates, 1,900 measurement rows and 2,956 bound assets; all 1,677 local report references resolved. Publication and Markdown checks passed.
- Browser review covered all four new regions: the circular target against d025 at 100%, the grayscale wedge in shadow recovery, the diagonal target at d200 with hard inversion, and the central target with the same-sequence RAW anchor. Crop switching and the PS16/RAW reference selector worked; browser warning/error logs were empty.

## Metadata audit supplement (local, 2026-09-17)

- The synthetic 64×64 RGB16 fixture uses fictional photographic metadata and the existing libjxl 0.11.2 binaries with ExifTool 13.12. Both lossless and d=0.05 preserve all 15 source-present curated fields after explicit copying. PPM retains none of the JXL's 24 photographic fields; the 16-bit PNG retains all 24. The generic diff exports field names and statuses, without metadata values or private paths.
- The lossless pixels and extracted ICC are exact. The lossy decoder returns a different ICC matrix/TRC profile. Independent Pillow inspection of both PNG outputs found embedded ICC bytes exactly equal to their corresponding `djxl --icc_out` files (748 bytes lossless; 636 bytes lossy). The candidate file hashes are recorded in the synthetic evidence.
- The supplement reuses the existing 16 real-image pilots, 160 rendered-JXL metadata/codestream audits and 32 DNG source/candidate comparisons. Against the preceding viewer-layout release, all 160 candidate records, 1,900 measurements, 2,953 image/viewer assets and original evidence sections remain unchanged. Only report packaging and the new metadata material change.
- The full readiness check passed 212 tests, Markdown links, publication audit and fixture provenance checks. The release gate passed; the report-site check found 0 errors among 1,677 local references. The available test packages were NumPy 2.5.3, Pillow 12.3.0, tifffile 2026.8.23 and imagecodecs 2026.8.16. The main image measurements were not rerun and retain their original environment identity.
- The new disclosure was confirmed collapsed on load, opened and closed with a click and Enter, and inspected on desktop and at a requested 390×844 viewport. At the mobile size, document width and scroll width both measured 375 pixels. Browser warning/error logs were empty; the viewport override was reset. Publication of this supplement is separate from the earlier report approval.

## Full-frame overview restoration

- The viewer's overview fields had been assigned the native-crop preview files. A dedicated downstream export now supplies 1,320 full-frame images: 22 approved crops × 12 image sources × five fixed modes. Each source is linked to its audited file hash and ICC profile. The yellow crop outline uses the source dimensions and the measured RAW registration.
- The first overview repair also changed the viewer layout. Following owner clarification, both viewer rendering files were restored exactly to the preceding documentation edition (`c086f61`). The upper overview images retain their original display size and placement; crop sizing, zoom, overlay and controls use the original implementation. The dedicated full-frame image files and their bindings are retained.
- The restored layout was inspected in the browser with Fuji `_DSC6938`, `manual-01`, using RAW61, PS16 and JPEG XL d=0.25, normal and hard-inversion views. The 1:1 control showed 100%; zoom reset and overlay switching worked. Browser console errors were absent. All 210 automated tests passed.
- A contact sheet covered the full-frame reference for all 22 crop selections. All 1,320 WebPs were decoded and checked for their 640-pixel maximum dimension and embedded ICC profile. Their combined size is 70.58 MiB. At a requested 390×844 mobile viewport, content and scroll width were both 375 pixels, and the native-scale control showed 100%.
- Comparison with the preceding documentation release confirmed all 1,611 existing image/pixel assets byte-identical, all 160 candidate records and 1,900 measurements unchanged, and the original analysis identity preserved. Publication checks now reject native-crop preview paths in overview fields and verify the final HTML bindings as well as the data manifests.

## Completed browser checks during the rebuild

- The f/4.5 target and Fuji `_DSC6938`, `manual-01`, were inspected using the RGB16 crop viewer. The Fuji transport test used byte-plane shuffle plus gzip, independently checked to reproduce every original sample.
- Fuji d=0.25 was inspected against the PS16 reference in normal, hard-density inversion and shadow-recovery modes. At d=2.0, highlight separation exposed coarser speckle and changes in small highlight boundaries. These observations are specific to the inspected crop and display; they do not establish a universal visibility threshold.
- The PS16/RAW61 reference selector, overlay, exposure at +1 EV, point-curve dragging and edit reset worked. An independent numerical test covers all five transformations and exposure changes; browser and Python calculations agree within the stated test tolerances.
- At a 390×844 requested mobile viewport (375-pixel content width with scrollbar), the page had no horizontal overflow. A layout defect that previously squeezed the canvas out of view was repaired. The editing panel starts collapsed; the image remains visible when controls wrap.
- A displayed-white counter defect was repaired: it now counts actual all-channel 8-bit display endpoints. Fuji shadow recovery reported 88.59% reference white and 88.61% d=0.25 candidate white in the tested view, consistent with its mostly white appearance. These are display endpoints, not sensor clipping.
- Server requests showed only the selected crop and adjacent quality levels being fetched, with a six-buffer pixel cache. No whole-corpus RGB16 prefetch occurred in those interactions. Browser console errors were absent.
- Kodak5035 `_DSC6619`, `manual-01`, was inspected at d=0.25 in normal and enlarged hard-inversion views, and at d=2.0 in hard inversion. The strong-loss control softened fine texture while retaining larger boundaries. A native-pixel control and scale readout were added; the one-source-pixel-per-display-pixel view was checked at 100%. This observation concerns that crop and is not a universal quality threshold.
- The regenerated public negative-target and Golden Gate panels were inspected for correct reference/candidate pairing, labels and normal/inversion rendering. The historical f/8 target and Kodak Gold combiner panels were inspected across normal, shadow and highlight views. These reduced panels illustrate appearance; their downsampling prevents conclusions about native-detail preservation.
- Konica `_DSC6917`, `manual-02`, was inspected against PS16 at d=0.30 and d=2.0 in hard inversion with shadow lift, at 100% display-pixel scale. The d=2.0 control showed coarser texture than the reference. Direct crop selection through a URL was also checked on this case.

## Completed release checks

- The full RGB rebuild completed all 16 frames and 160 candidates. The release gate validated 1,900 measurement rows, 22 crop manifests and 1,635 bound assets, together with all six auxiliary evidence sections. Full retained source and original/repaired encode hashes were rechecked before release assembly.
- The complete regression suite passed: 203 tests. This includes six release-mutation tests that reject a relaxed budget, wrong cohort, altered recipe, missing measurement, changed viewer profile and changed browser transformation. The publication audit found no issues; local Markdown links resolved.
- A clean environment containing the six public fixtures and public code, with no private inventory or input tree, reproduced all 24 candidates and 120 transformation measurements. All 2,764 compared numerical values matched exactly. One grayscale lossless file differed by two encoded bytes because its generated ICC creation timestamp changed; decoded pixels and the full color interpretation matched. The comparator records this permitted difference explicitly.
- The complete unchanged-run check reused all 16 frames while every heavy preparation, render, encode and decode function was blocked. A separate partial-interruption regression completed one distance, interrupted the next, and preserved the completed checkpoint on restart despite changing ICC creation timestamps. The regression failed without the preview adapter and passed with it; changed pixels, profile data and corrupt PNGs are rejected.
- Viewer export journals passed interruption/idempotency tests. Bounded retries resolved Windows sharing locks during packaging; access-denied and permanent-lock tests still fail rather than retry indefinitely. The packed transport preserves every uint16 sample and is checked by both file and decoded-pixel SHA-256.
- The lineage audit verified all 160 original/repaired image codestreams equal, reproduced all 22 aligned RAW viewer buffers and all 220 native identity high-pass losses, and found no change in 220 relative decisions after removing the padding-dependent boundary pixels. RAW viewer quantization reached four sRGB display codes across the checked five modes at zero EV; normal-mode error was at most one code. Scientific measurements retain the floating-point RAW input.
- Superseded report derivatives were removed only after the replacement passed: 1,550 obsolete assets totaling 755.41 MiB. That initial site was about 877 MiB. Original captures, retained renders, encodes and historical results remain retained.

## Final assembled-page review

- The desktop report was inspected for readable tables, matching headline counts and equal prominence of the RAW61 and DNG routes. The d=2.0 controls also pass the relative screens despite visible changes in inspected crops; the report now states explicitly that these screens cannot define acceptable archival quality.
- The actual standalone Konica `_DSC6917`, `manual-02`, redirect selected that exact capture and crop. Its PS16-reference versus d=0.30 hard-inversion-with-shadow-lift view rendered at 100% physical-pixel scale on the final page.
- At a requested 390×844 mobile viewport, content width and viewport width both measured 375 pixels. The summary cards and viewer stayed within the page; the image remained visible under wrapping controls. The temporary viewport override was reset afterward.
- The CSV link produced a browser download event. The browser automation's event check for the larger JSON download timed out; independent HTTP checks returned status 200 and byte-identical content for all ten data/documentation links, including the complete JSON and CSV. This records the automation limit without claiming an observed JSON download event.
- The final page reported no browser console errors or warnings in the checked interactions. Server requests for RGB16 transport remained confined to the selected crops and adjacent quality levels. Canceled obsolete requests can appear as connection resets in the local server log.

Owner review and initial publication are complete. The [current report](https://hsnilsson.github.io/bayer-raw-vs-pixel-shift-jpeg-xl/) lists the remaining scientific work, including stronger visual acceptance criteria and the common DNG/RAW61 quality comparison. Original source masters remain retained.
